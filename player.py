from datetime import timedelta


class Player:
    """A class to store player's name, session date and duration"""
    def __init__(self):
        self.player = input("Hello user. What's your name?")
